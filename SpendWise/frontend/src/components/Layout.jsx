import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {LayoutDashboard,Receipt,Tags,Wallet,BarChart3,User,Settings,LogOut,Plus,Menu} from 'lucide-react';import {useAuth} from '../context/AuthContext';import {useState} from 'react';
export default function Layout(){const {logout,user}=useAuth();const nav=useNavigate();const [open,setOpen]=useState(false);const links=[['/dashboard','Overview',LayoutDashboard],['/expenses','Transactions',Receipt],['/expenses/add','Add expense',Plus],['/categories','Categories',Tags],['/budget','Budget',Wallet],['/analytics','Analytics',BarChart3],['/profile','Profile',User],['/settings','Settings',Settings]];return <div className="shell"><div className={`scrim ${open?'show':''}`} onClick={()=>setOpen(false)}/><aside className={open?'open':''}><div className="brand"><span className="brandmark">◈</span><span>SpendWise</span></div><small className="tagline">Your money, in focus.</small><nav>{links.map(([to,t,I])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><I size={18}/><span>{t}</span>{t==='Add expense'&&<em>+</em>}</NavLink>)}</nav><div className="sidefoot"><div className="profilechip"><div className="avatar">{user?.username?.[0]?.toUpperCase()||'S'}</div><div><b>{user?.username||'Guest'}</b><small>Personal account</small></div></div><button className="logout" onClick={()=>{logout();nav('/login')}}><LogOut size={17}/> Sign out</button></div></aside><main><header><button className="menu" aria-label="Open menu" onClick={()=>setOpen(true)}><Menu size={21}/></button><div><b>Personal Finance</b><span className="crumb"> / {user?.username}</span></div><div className="headeractions"><span className="statusdot"/> <span>Synced just now</span><button className="headeradd" onClick={()=>nav('/expenses/add')}><Plus size={17}/> Add expense</button></div></header><section className="content"><Outlet/></section></main></div>}
